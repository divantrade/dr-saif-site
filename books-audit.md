# نتائج استكشاف محتوى الكتب والدراسات

هذا التقرير يجيب على السؤال الذي طرحه البرومبت قبل بناء قسم الكتب في الصفحة
الرئيسية: ما المتوفّر فعلاً من بيانات الكتب في Sanity، وهل لها صور أغلفة،
وهل لها صفحات منفصلة.

## 1. هل يوجد content type مستقل للكتب في Sanity؟

لا. ملف `sanity/schemas/index.ts` يعرّف الأنواع التالية فقط:

```
post, intellectualAxis, series, category, tag, author
```

لا يوجد `book` ولا `study` ولا `publication`. يجب إنشاؤه.

## 2. هل الكتب موجودة كمقالات مصنَّفة تحت «الكتب»؟

نعم. في البيانات المُرحَّلة من WordPress (التي هي نفسها في Sanity الآن
عبر جسر `legacyId`):

| التصنيف | `legacyId` | عدد المقالات |
| --- | --- | --- |
| إضاءات حضارية / الكتب | 41 | 13 |
| إضاءات حضارية / الدراسات | 43 | 61 |
| في الكتب والدراسات المعاصرة | 39 | 18 |

التصنيف 41 («الكتب») هو المصدر الذي نستخرج منه قائمة الكتب الأساسية.

### قائمة الـ 13 كتاباً الموجودة في التصنيف 41

```
2025  ملخص صوتي لكتاب قاموس المقاومة
2025  كتاب عقلية الوهن: دراسة لأزمة الخليج
2024  كتاب في النظرية السياسية (1998)
2024  فتاوى الأمة وأصول الفقه الحضاري
2024  المجتمع المدني وأبعاده الفكرية
2023  المرحلة الانتقالية: قراءة في المشهد المصري — طبعة 2014
2023  المداخل المنهاجية للبحث في العلاقات الدولية في الإسلام — 1996
2023  بناء المفاهيم: دراسة معرفية ونماذج تطبيقية — 1998
2023  العلاقات الدولية في الإسلام: مدخل القيم — 1999
2023  المشروع الحضاري الإسلامي للتغيير — طبعة 2013
2023  النظرية السياسية من منظور حضاري إسلامي — طبعة 2002
2023  العولمة والإسلام: رؤيتان للعالم — طبعة 2009
2023  الزحف غير المقدس: تأميم الدولة للدين — طبعة 2016
```

## 3. هل توجد صور أغلفة؟

كل الـ 13 كتاباً في تصنيف 41 لها `featured_media` في بيانات WordPress
المُرحَّلة (13/13). ما إذا كانت هذه الصور قد رُفعت كـ Sanity images
في حقل `featuredImage` يعتمد على سكربت الهجرة، وقد يكون بعضها لم
يُرفع. لذلك الأمان المتبع هو: إذا وُجدت صورة نعرضها، وإلا نعرض خلفية
ملوّنة بلون المحور (نفس حل الصور المفقودة في بطاقات المقالات).

## 4. هل للكتب صفحات منفصلة اليوم؟

- `/books` — صفحة موجودة لكنها تعتمد على بيانات WordPress المحلية (ملف
  `public/data/posts.json`) بدل Sanity، وتستعمل مكوّن `PostCard` — أي
  تعرض الكتب بصيغة «مقال» لا بصيغة «كتاب». تحتاج إعادة بناء.
- كل كتاب له رابط `/blog/{slug}` مستقلّ (لأنه post).

## 5. الخطة المعتمَدة

استناداً لما سبق، نمضي في السيناريوهين الأول والثاني معاً:

1. **ننشئ نوع `book` جديد في Sanity schema** — بالحقول: `title, subtitle,
   year, publisher, coverImage, description, axis, order, slug, isbn`.
   هذا يُعطي المحرّر مستقبلاً مكاناً نظيفاً لإدخال الكتب بحقول الكتاب
   الحقيقية (لا حقول المقال).

2. **نكتب `getFeaturedBooks()` في `lib/sanity-data.ts`** — يقرأ أولاً
   من `*[_type == "book"]`. إن كانت فارغة يسقط على قائمة ثابتة مضمَّنة
   في الكود تمثّل السبعة كتب التأسيسية للمشروع (كما في البرومبت).

3. **نبني مكوّن `BooksShowcase`** يعرض الكتب كصفّ من الأغلفة (2:3) —
   إمّا صورة غلاف إن وُجدت، وإلاّ لوحة ملوّنة بلون المحور مع pattern
   هندسي — يتبع الزرّ «كل الكتب والدراسات ←».

4. **نُعيد كتابة صفحة `/books`** لتستخدم نفس المصدر، بتصميم يليق بالكتب
   (لا مقالات)، وتضم قسماً فرعياً «دراسات مختارة» يسرد روابط نصية.

البيانات الثابتة المضمَّنة هي السبع التأسيسية (على نسق البرومبت):

1. النظرية السياسية من منظور حضاري إسلامي (2002) — محور 1
2. الزحف غير المقدس: تأميم الدولة للدين (2016) — محور 4
3. العولمة والإسلام: رؤيتان للعالم (2009) — محور 5
4. المشروع الحضاري الإسلامي للتغيير (2013) — محور 3
5. فتاوى الأمة وأصول الفقه الحضاري — محور 1
6. المداخل المنهاجية للبحث في العلاقات الدولية في الإسلام (1996) — محور 5
7. العلاقات الدولية في الإسلام — مدخل القيم (1999) — محور 5

الدراسات المختارة (بدون أغلفة) مُشتقّة من التصنيف 43:

- نحو بناء مقياس للفساد: رؤية من منظور المدرسة الخلدونية (2008)
- أركان الرؤية السياسية الإسلامية (2017)
- التربية المدنية: دراسة في المفهوم بين العالمية والخصوصية (2007)
